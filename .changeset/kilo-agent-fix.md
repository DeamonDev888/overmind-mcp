---
"overmind-mcp": minor
---

- Add `cwd` parameter to run_agent for ClaudeRunner and KiloRunner
- Add `$VAR` environment variable substitution in agent settings
- Fix workspace directory resolution for agent settings
- Add ANTHROPIC_AUTH_FALLBACK fields as resolvable references
- Update documentation with new features
